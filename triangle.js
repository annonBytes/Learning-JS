//Testing comments
//AUTHOR: big bytes
/*Following the example of  listing out numbers, i just replaced the number value with a '#'and 
included the new line "\" */

for (var hash = "#"; hash.length < 8; hash += "#")
    console.log(hash);
